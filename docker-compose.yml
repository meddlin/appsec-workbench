services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: appsec_local
      POSTGRES_USER: appsec
      POSTGRES_PASSWORD: appsec
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
